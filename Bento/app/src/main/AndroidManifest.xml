<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aishalien.bento">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_test"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_test"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main_menu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".profile"
            android:label="@string/profile"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".profile_modify"
            android:label="@string/profile_modify"
            android:theme="@style/AppTheme.NoActionBar" />
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".order_meal" />
        <activity android:name=".content_main_menu" />
        <activity android:name=".store_menu" />
        <activity android:name=".meal_purchase" />
        <activity android:name=".application_fillment" />
        <activity
            android:name=".shopping_cart"
            android:label="@string/title_activity_shopping_cart"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".modification_shopping_car"></activity>

    </application>

</manifest>